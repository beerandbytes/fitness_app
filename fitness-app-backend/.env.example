# ============================================
# FITNESS APP BACKEND - Variables de Entorno
# ============================================
# Copia este archivo a .env y configura los valores según tu entorno
# cp .env.example .env

# ============================================
# VARIABLES CRÍTICAS (REQUERIDAS)
# ============================================

# URL de conexión a PostgreSQL
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_base_datos
# Ejemplo local: postgresql://postgres:password@localhost:5432/fitnessdb
# Ejemplo Docker: postgresql://fitnessuser:password@postgres:5432/fitnessdb
DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/fitnessdb

# Secreto para firmar tokens JWT
# IMPORTANTE: Debe tener al menos 32 caracteres para mayor seguridad
# Genera uno seguro con: openssl rand -base64 32
JWT_SECRET=tu-secret-key-super-segura-y-larga-minimo-32-caracteres-aqui

# ============================================
# VARIABLES RECOMENDADAS
# ============================================

# Puerto del servidor backend
PORT=4000

# Entorno de ejecución (development, production, test)
NODE_ENV=development

# URL del frontend (para CORS y enlaces de invitación)
FRONTEND_URL=http://localhost:5173
FRONTEND_BASE_URL=http://localhost:5173

# ============================================
# VARIABLES OPCIONALES
# ============================================

# Secreto para refresh tokens (si no se define, usa JWT_SECRET)
JWT_REFRESH_SECRET=

# Host del servidor (por defecto: 0.0.0.0)
HOST=0.0.0.0

# Lista de emails separados por comas que serán administradores automáticamente
# Ejemplo: admin@ejemplo.com,coach@ejemplo.com
ADMIN_EMAILS=

# Habilitar Swagger en producción (por defecto: false)
ENABLE_SWAGGER=false

# Modo debug para administradores (por defecto: false)
DEBUG_ADMIN=false

# ============================================
# CONFIGURACIÓN SMTP (Opcional)
# Para recuperación de contraseña y envío de invitaciones
# ============================================

# Servidor SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false

# Credenciales SMTP
SMTP_USER=
SMTP_PASS=

# Email remitente
SMTP_FROM=noreply@fitnessapp.com

# ============================================
# CONFIGURACIÓN DE LOGGING (Opcional)
# ============================================

# Nivel de logging (error, warn, info, debug)
LOG_LEVEL=info

# ============================================
# NOTAS
# ============================================
# - Las variables críticas (DATABASE_URL, JWT_SECRET) son obligatorias
# - JWT_SECRET debe tener al menos 32 caracteres
# - Para Gmail, necesitas crear una "Contraseña de aplicación" en tu cuenta de Google
# - En producción, nunca commitees el archivo .env real
# ============================================
